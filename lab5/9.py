import re  # Подключаем модуль регулярных выражений

def camel_to_snake(text):
    # Используем re.sub() для вставки "_" перед заглавными буквами, кроме первой
    return re.sub(r'(?<!^)(?=[A-Z])', '_', text).lower()

# Примеры работы
print(camel_to_snake("camelCaseExample"))  # ✅ 'camel_case_example'
print(camel_to_snake("thisIsATest"))       # ✅ 'this_is_a_test'
print(camel_to_snake("PythonProgramming")) # ✅ 'python_programming'

# 🔹 Разбор шаблона r'(?<!^)(?=[A-Z])'
# (?<!^)  → "Отрицательный просмотр назад" (negative lookbehind)
#           Убеждается, что перед заглавной буквой НЕТ начала строки (^).
#           Это значит, что первая заглавная буква остается без изменений.
#
# (?=[A-Z]) → "Положительный просмотр вперед" (positive lookahead)
#             Находит заглавные буквы (A-Z), но не включает их в совпадение.
#             Это позволяет добавить "_" перед ними, не заменяя сами буквы.
#
# .lower()  → Преобразует всю строку в нижний регистр после вставки "_".